""" This file stores settings for courseparser and other scripts """

# Storage solution for Shove
# Possible options are:
#   simple, file, memory, dbm, bsddb, sqlite, mssql, postgres, firebird, mysql,
#   oracle, svn, s3, zodb, ftp, durus
store = "memory://"
# Cache solution for Shove
# Possible options are:
#   simple, memory, fileru, file, memcached, bsddb, mssql, postgres, firebird,
#   mysql, oracle
cache = "memory://"
